1.Given a string s consisting of words and spaces, return the length of the last word in the string.
def last_word(s):
    words = s.split()
    if not words:
        return 0
    return len(words[-1])

string = input()
result = last_word(string)
print(result)
